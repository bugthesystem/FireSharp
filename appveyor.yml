image: Visual Studio 2019

configuration: Release
    
before_build:
  - nuget restore FireSharp.Core.sln

build:
  project: FireSharp.Core.sln       
  verbosity: minimal

artifacts:

  # pushing all *.nupkg files in build directory recursively
  - path: '**\FireSharp*.nupkg'
  - name: nuget
